import { RequestHolder, PresentedCompactSdJwt } from "../types";
declare const holder: (options?: RequestHolder) => {
    issue: ({ token, disclosure, audience, nonce }: {
        token: string;
        disclosure: string;
        audience?: string | string[] | undefined;
        nonce?: string | undefined;
    }) => Promise<PresentedCompactSdJwt>;
};
export default holder;
