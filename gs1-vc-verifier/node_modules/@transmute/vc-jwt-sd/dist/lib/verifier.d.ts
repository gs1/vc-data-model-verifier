import { RequestVerifier, VerifiedSdJwt } from '../types';
export default function verifier<T = VerifiedSdJwt>(options: RequestVerifier): {
    verify: ({ token, audience, nonce }: {
        token: string;
        audience?: string | undefined;
        nonce?: string | undefined;
    }) => Promise<T>;
};
