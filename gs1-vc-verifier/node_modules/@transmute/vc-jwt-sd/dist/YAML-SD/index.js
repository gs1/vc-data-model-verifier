"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const yaml_1 = require("yaml");
const parseCustomTags_1 = require("./parseCustomTags");
const yamlOptions_1 = require("./yamlOptions");
const issuancePayload_1 = require("./issuancePayload");
const disclose_1 = require("./disclose");
const dumps = (data) => {
    return (0, yaml_1.stringify)(data, yamlOptions_1.yamlOptions);
};
const roughlyEqual = (a, b) => {
    return JSON.stringify((0, yaml_1.parse)(a)) === JSON.stringify((0, yaml_1.parse)(b));
};
const load = (data) => {
    const parsedData = (0, parseCustomTags_1.parseCustomTags)(data).contents;
    if (parsedData === null) {
        throw new Error('parsed data cannot be null.');
    }
    return parsedData;
};
const YAML = {
    load,
    issuancePayload: issuancePayload_1.issuancePayload,
    parseCustomTags: parseCustomTags_1.parseCustomTags,
    loads: yaml_1.parse,
    dumps,
    disclose: disclose_1.disclose,
    roughlyEqual,
};
exports.default = YAML;
