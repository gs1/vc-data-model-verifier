import { PublicKeyJwk } from '../cose/sign1';
declare const verifier: ({ publicKeyJwk }: {
    publicKeyJwk: PublicKeyJwk;
}) => {
    verify: (toBeSigned: ArrayBuffer, signature: ArrayBuffer) => Promise<ArrayBuffer>;
};
export default verifier;
