import { PublicKeyJwk, RequestCoseSign1DectachedVerify } from "../sign1";
export type RequestHeaderVerifier = {
    resolve: (signature: ArrayBuffer) => Promise<PublicKeyJwk>;
};
export declare const verifier: (opt: RequestHeaderVerifier) => Promise<{
    verify: (req: RequestCoseSign1DectachedVerify) => Promise<{
        payload: ArrayBuffer;
        receipts: ArrayBuffer[];
    }>;
}>;
