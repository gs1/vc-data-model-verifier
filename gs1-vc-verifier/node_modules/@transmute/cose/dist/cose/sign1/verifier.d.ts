import { RequestCoseSign1Verifier, RequestCoseSign1Verify } from './types';
declare const verifier: ({ resolver }: RequestCoseSign1Verifier) => {
    verify: ({ coseSign1, externalAAD }: RequestCoseSign1Verify) => Promise<ArrayBuffer>;
};
export default verifier;
