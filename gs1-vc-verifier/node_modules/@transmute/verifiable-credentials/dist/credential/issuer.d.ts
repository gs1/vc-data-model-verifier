import { RequestCredentialIssuer, RequestIssueCredential } from '../types';
export declare const issuer: (issuer: RequestCredentialIssuer) => {
    issue: (credential: RequestIssueCredential) => Promise<Uint8Array>;
};
