import { VerifiableCredential, VerifiablePresentation, RequestVerifier, RequestVerify } from '../types';
export declare const verifyUnsecuredPresentation: ({ resolver }: RequestVerifier, { content, audience, nonce }: RequestVerify) => Promise<any>;
export declare const verifier: ({ resolver }: RequestVerifier) => {
    verify: <T = VerifiableCredential | VerifiablePresentation>({ type, content, audience, nonce }: RequestVerify) => Promise<T>;
};
