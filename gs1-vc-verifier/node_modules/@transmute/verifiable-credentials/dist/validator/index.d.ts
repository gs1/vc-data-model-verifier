import { RequestValidator, SecuredContentType, TraceablePresentationValidationResult } from "../types";
export declare const validator: ({ resolver }: RequestValidator) => {
    validate: <T = TraceablePresentationValidationResult>({ type, content }: SecuredContentType) => Promise<T>;
};
