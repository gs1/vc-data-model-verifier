import { RequestPresentationHolder, RequestCredentialPresentation } from "../types";
export declare const holder: (holder: RequestPresentationHolder) => {
    issue: (req: RequestCredentialPresentation) => Promise<Uint8Array>;
};
