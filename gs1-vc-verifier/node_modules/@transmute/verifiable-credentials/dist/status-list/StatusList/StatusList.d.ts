import { Bitstring } from './Bitstring';
import { CreateStatusList, UpdateStatusList, CheckStatusList } from './types';
export declare class StatusList {
    static Bitstring: typeof Bitstring;
    static defaultLength: number;
    static create: ({ id, length, purpose, }: CreateStatusList) => Promise<any>;
    static updateStatus: ({ claimset, position, purpose, status, }: UpdateStatusList) => Promise<any>;
    static checkStatus: ({ claimset, purpose, position, }: CheckStatusList) => Promise<boolean>;
}
