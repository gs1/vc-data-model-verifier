import { Bitstring } from './StatusList/Bitstring';
type RequestCreateStatusList = {
    id: string;
    purpose: 'revocation' | 'suspension';
    issuer: string;
    validFrom: string;
    validUntil?: string;
};
export declare const create: ({ issuer, validFrom, validUntil, id, purpose, }?: RequestCreateStatusList) => Promise<string>;
declare class BS {
    bs: Bitstring;
    constructor(data: number | string);
    set(position: number, bit: boolean): this;
    get(position: number): boolean;
    encode(): Promise<string>;
}
export declare const bs: (data: number | string) => BS;
export {};
